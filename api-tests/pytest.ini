[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*

markers =
    smoke: critical tests that validate the main API flows
    regression: broader test suite for existing functionality
    negative: tests for invalid inputs or missing/invalid headers
