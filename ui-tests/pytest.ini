[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*

markers =
    smoke: critical UI flows (screen loads, core happy paths)
    regression: broader test suite for existing functionality
